---
시험: 2025-3회-필기
문번: 65
과목: 프로그래밍 언어 활용
정답: ①
추출도구: claude
추출일: 2026-07-22
---
다음은 n각형을 화면에 그리는 프로그램을 Python으로 구현한 것이다. 괄호(㉠~㉢)에 들어갈 알맞은 코드는?

```python
import turtle
( ㉠ ) shape(distance, n):
   t = turtle.Turtle( )
   for i in range(n):
      t.( ㉡ )
      t.( ㉢ )
shape(100, 5)
```

① def, forward(distance), left(360//n)
② def, forward(distance), left(360///n)
③ class, forward(distance), left(360//n)
④ class, forward(distance), left(360///n)
