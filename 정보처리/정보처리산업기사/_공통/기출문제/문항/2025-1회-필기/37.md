---
시험: 2025-1회-필기
문번: 37
과목: 프로그래밍 언어 활용
정답: ②
추출도구: claude
추출일: 2026-07-22
---
다음 코드를 조건 연산자로 올바르게 구현한 것은?

```c
if (a > 5)
    a = a * 2;
else
    a = a * 3;
```

① a > 5 ? a *= 2 : a *= 3;
② a *= a > 5 ? 2 : 3;
③ a = a > 5 ? 2 : 3;
④ a = a > 5 ? a * 3 : a * 2;
